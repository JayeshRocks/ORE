# Mirror of pyproject.toml [project.dependencies].
# Prefer `pip install -e ".[dev]"` for development; this file exists so
# plain `pip install -r requirements.txt` still works for non-editable installs.

pymupdf>=1.24.0
langchain-text-splitters>=0.3.0
ollama>=0.3.0
chromadb>=0.5.0
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9
